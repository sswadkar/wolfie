import { Figtree, Inter } from 'next/font/google'

export const figtree = Figtree(
    {
        subsets: ['latin'],
        display: 'swap'

    }
)

export const inter = Inter(
    {
        subsets: ['latin'],
        display: 'swap'

    }
)